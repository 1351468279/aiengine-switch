# Security

请不要在公开 Issue、日志或截图中提交 AIARE API 密钥。安全问题请优先使用 GitHub 仓库的私密安全报告功能，并提供可复现步骤、受影响版本和影响范围。

安装器的安全边界：

- 下载的发布包必须通过同一发布源提供的 SHA-256 校验清单。
- API 密钥通过终端隐藏输入，保存在安装器专用凭据文件中。
- Claude Code 与 Codex 通过凭据助手按需读取密钥；配置和状态文件不保存密钥。
- Codex 的 `auth.json` 不受本工具管理。
- 配置修改采用临时文件和原子替换，失败时恢复本次修改前的文件。

GitHub Release 当前只提供校验和，未提供代码签名。需要更高供应链保证的环境应从固定提交自行构建，并通过独立可信渠道核对提交和校验值。
